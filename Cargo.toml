[package]
name = "oci-cache"
version = "0.0.0"
edition = "2021"
# 依存のうち最も新しい版を要求するのが redb 4.2（1.90）。次いで jsonwebtoken と
# icu_* が 1.88、idna_adapter が 1.86。ここを下げるには依存を古い版へ固定する
# 必要があり、redb 4 は索引の前提（README の技術選定）なので下げない。
# この値が実際に守られているかは ci.yml の msrv ジョブが確かめる
rust-version = "1.90"
description = "複数の上流レジストリに対応する、パス透過なプルスルーキャッシュ"
license = "Apache-2.0"
repository = "https://github.com/windschord/oci-cache"
readme = "README.md"

[dependencies]
# HTTP サーバ
axum = { version = "0.8", features = ["http2", "macros"] }
# "time" は上流への問い合わせを打ち切るのに使う tokio::time::timeout に要る（REQ-0057 / REQ-0059）
tokio = { version = "1", features = ["rt-multi-thread", "macros", "fs", "signal", "time"] }
tower = "0.5"
tower-http = { version = "0.7", features = ["trace", "set-header"] }
hyper = "1"

# 上流レジストリとの通信（REQ-0051: ストリーム取得が必須要件）
oci-client = "0.17"
reqwest = { version = "0.13", default-features = false, features = ["rustls", "stream"] }

# 保存とインデックス（REQ-0020 / REQ-0050）
redb = "4"
sha2 = "0.11"
hex = "0.4"

# UI の同梱（REQ-0043）
rust-embed = { version = "8", features = ["mime-guess"] }

# 設定・直列化
serde = { version = "1", features = ["derive"] }
serde_json = "1"
figment = { version = "0.10", features = ["toml", "env"] }

# 観測（REQ-0044）
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
metrics = "0.24"
metrics-exporter-prometheus = "0.18"

# 誤り処理
thiserror = "2"
anyhow = "1"

[dev-dependencies]
tokio = { version = "1", features = ["test-util"] }
wiremock = "0.6"
tempfile = "3"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
